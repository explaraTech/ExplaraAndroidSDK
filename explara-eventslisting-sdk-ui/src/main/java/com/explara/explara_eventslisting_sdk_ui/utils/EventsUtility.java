package com.explara.explara_eventslisting_sdk_ui.utils;

/**
 * Created by ananthasooraj on 15/04/16.
 */
public class EventsUtility {
    /*public static Intent generateTicketPageIntent(Context context, String eventId) {

        Intent intent;
        if (ConstantKeys.EVENT_SESSION_TYPE.THEATER.equals(EventsManger.newInstance().eventsDetailDtoMap.get(eventId).events.getEventSessionType())) {
            intent = new Intent(context, TicketsDetailWithDatesActivity.class);
        } else if (ConstantKeys.EVENT_SESSION_TYPE.CONFERENCE.equals(EventsManger.newInstance().eventsDetailDtoMap.get(eventId).events.getEventSessionType())) {
            intent = new Intent(context, TicketsDetailWithMultipleSessionActivity.class);
        } else if (ConstantKeys.EVENT_SESSION_TYPE.MULTIDATE.equals(EventsManger.newInstance().eventsDetailDtoMap.get(eventId).events.getEventSessionType())) {
            intent = new Intent(context, MultidatePackagesActivity.class);
        } else {
            intent = new Intent(context, TicketsDetailActivity.class);
        }
        return intent;
    }*/

}
